```js\n/* ---------- Table entry ---------- /\ndocument.getElementById('table-submit').addEventListener('click', () => {\n const table = document.getElementById('table-input').value.trim();\n if (table && +table > 0 && +table < 100) {\n document.getElementById('table-modal').classList.add('hidden');\n document.getElementById('app').classList.remove('hidden');\n sessionStorage.setItem('kfb_table', table);\n } else {\n alert('Please enter a valid table number (1‑99).');\n }\n});\n\n/ ---------- Render menu ---------- /\nfunction renderMenu() {\n const menuContainer = document.getElementById('menu');\n MENU.forEach(section => {\n const secTitle = document.createElement('h2');\n secTitle.textContent = section.category;\n menuContainer.appendChild(secTitle);\n\n const grid = document.createElement('div');\n grid.className = 'grid';\n section.items.forEach(item => {\n const card = document.createElement('div');\n card.className = 'card';\n card.innerHTML = \n <img src=\"${item.img}\" alt=\"${item.name}\">\n <strong><span class=\"icon ${item.veg ? 'veg' : 'nonveg'}\"></span>${item.name}</strong><br>\n ₹${item.price}<br>\n <button data-id=\"${item.id}\">Add</button>\n ;\n grid.appendChild(card);\n });\n menuContainer.appendChild(grid);\n });\n}\n\n/ ---------- Cart logic ---------- /\nlet cart = {};\n\nfunction updateCartUI() {\n const countEl = document.getElementById('cart-count');\n const totalEl = document.getElementById('cart-total');\n const itemsEl = document.getElementById('cart-items');\n\n let total = 0;\n let count = 0;\n itemsEl.innerHTML = '';\n\n Object.entries(cart).forEach(([id, entry]) => {\n const li = document.createElement('li');\n li.textContent = ${entry.name} × ${entry.qty} = ₹${entry.price * entry.qty};\n if (entry.note) li.textContent += (Note: ${entry.note});\n itemsEl.appendChild(li);\n total += entry.price * entry.qty;\n count += entry.qty;\n });\n\n countEl.textContent = count;\n totalEl.textContent = ₹${total};\n}\n\n/ ---------- Event listeners ---------- */\ndocument.addEventListener('click', e => {\n // Add item\n if (e.target.matches('button[data-id]')) {\n const id = e.target.dataset.id;\n const item = MENU.flatMap(s => s.items).find(i => i.id === id);\n if (!item) return;\n // Prompt for special instructions (optional)\n const note = prompt('Any special instructions? (Leave blank for none)', '');\n cart[id] = cart[id]
